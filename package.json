{
  "name": "gym-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && dotnet run --urls=http://localhost:5000",
    "start:frontend": "cd frontend && ng serve",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  }
}
